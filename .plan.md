# 中小屏幕 UI 适配方案

## 问题分析

当前项目在酒馆 iframe 中运行，所有组件均基于大屏设计：
- `TitleScreen` 使用 `aspect-ratio: 3/4` 固定比例 + 绝对定位，18个粒子动画持续运行
- `CreationScreen` 同样 `aspect-ratio: 3/4` + 绝对定位 footer
- `MainGame` 状态栏内各 panel 间距/字号偏大
- `PlayerPanel` 身体部位单列排列占用过多纵向空间
- 所有组件缺少 `@media` 断点适配

## 适配策略

### 断点定义
- **小屏**: `max-width: 400px`（手机竖屏 / 酒馆侧边栏窄模式）
- **中屏**: `max-width: 600px`（手机横屏 / 小平板）

### 1. index.html 修复
- 恢复为纯 `<body>` 结构（酒馆 iframe 自带 head）
- viewport meta 由酒馆外层控制，不在 iframe 内重复声明

### 2. TitleScreen.vue — 性能 + 布局重构
**性能优化：**
- 粒子数从 18 减到 8（小屏）— 用 CSS `@media` 配合 `display: none` 隐藏多余粒子
- 移除 `cornerPulse` 无限循环动画，改为静态显示
- `hintPulse` 无限动画改为 `animation-iteration-count: 3`（播放3次后停止）
- `charDrop` 一次性动画保留（不影响性能）

**布局调整：**
- 去掉 `aspect-ratio: 3/4`，改为 `min-height: 100vh`（iframe 内等于全屏）或 `min-height: 100%`
- 绝对定位改为 flex column 流式布局，auto 间距
- 名字输入框宽度 `width: min(180px, 70%)`
- 标题字号：42px → `clamp(28px, 8vw, 42px)`

### 3. CreationScreen.vue — 布局重构
**布局调整：**
- 去掉 `aspect-ratio: 3/4`，改为正常流式布局 + `overflow-y: auto`
- `.content` 区域使用 `flex: 1; overflow-y: auto` 自适应高度
- `.footer-actions` 从绝对定位改为粘性底部 `position: sticky; bottom: 0`
- 进度条 step-dot 缩小：22px → 18px（小屏）
- 阵营按钮网格：小屏下 3 列

**性能优化：**
- `slide` 过渡动画在小屏使用 `opacity` 替代 `translateX`（减少重排）

### 4. MainGame.vue — 紧凑模式
- 正文区域 padding 缩减：12px 14px → 8px 10px（小屏）
- 正文字号：14px → 13px，行高 1.9 → 1.7
- 状态栏 padding: 8px → 6px
- 分隔线 margin 缩减

### 5. TabNav.vue — 小屏折叠
- 小屏下隐藏 `.tab-label`，只显示 icon
- tab 按钮 `min-width: 48px → 36px`
- gap: 3px → 2px

### 6. PlayerPanel.vue — 两列身体 + 紧凑布局
- **身体部位**：`.body-list` 改为 `display: grid; grid-template-columns: 1fr 1fr`
- 信息条 label 宽度缩减
- 装备网格维持两列
- 属性网格：3列保持（已经够紧凑）
- 各 section margin-bottom 缩减

### 7. CompanionPanel / NpcPanel — 紧凑卡片
- 小屏下 card padding 缩减
- summary 信息 `flex-wrap: wrap`
- 展开详情字号缩小

### 8. InventoryPanel — 无大改
- 小屏下缩减 padding

### 9. MapPanel — 区域按钮网格
- 小屏下 district-btn padding 缩减
- grid-wrap 维持 flex-wrap（已适配）

### 10. HistoryPanel — 无大改
- floor-list max-height 保持

## 实施顺序

1. 修复 `index.html`（恢复正确结构）
2. 重写 `TitleScreen.vue`（去粒子性能问题 + 流式布局）
3. 重写 `CreationScreen.vue`（去固定宽高比 + sticky footer）
4. 修改 `MainGame.vue`（响应式间距）
5. 修改 `TabNav.vue`（小屏只显示 icon）
6. 修改 `PlayerPanel.vue`（身体两列 + 紧凑）
7. 修改 `CompanionPanel.vue` / `NpcPanel.vue`（紧凑卡片）
8. 修改 `MapPanel.vue` / `InventoryPanel.vue` / `HistoryPanel.vue`（微调）
9. 重新 build 并提交

## 全屏模式考虑
酒馆 iframe 全屏时宽度可能很窄（侧边状态栏），所有组件以 `width: 100%` 自适应为原则，不写死任何固定宽度。关键：
- 不使用 `aspect-ratio` 强制比例（让内容自然撑高）
- 所有绝对定位改为流式/flex
- 文本自动换行 `word-break: break-all` 用于超长名称
